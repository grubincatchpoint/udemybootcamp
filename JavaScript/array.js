//array.unshift will add to the beginning of the array
//array.shift will remove first item in array
//array.push will add to the end
//array.pop will remove the last element of array
//array.indexOf(el) will provide the index of the element, -1 if not present
//array.slice(indexStart, indexStop) will cut items out of current array
//array.slice() creates a duplicate array
